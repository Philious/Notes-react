export enum Page {
  LOGIN = '/',
  MAIN = '/main',
}

export const scratch = 'scratch'

export enum ButtonType {
  Default = 'default',
  Filled = 'filled',
  Border = 'border'
}

export enum Icon {
  Add = 'add',
  Cancel = 'cancel',
  Up = 'up',
  Left = 'left',
  Down = 'down',
  Right = 'right',
  List = 'list',
  Options = 'options',
  Setting = 'setting',
  Check = 'check',
  Remove = 'remove',
  LetterSize = 'letter-size',
  LogOut = 'logout'
}
